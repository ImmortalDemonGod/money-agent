# Addendum — 2026-07-20

**Forward-only.** Run 1's conclusions are left exactly as the run reached them. This file records
what became known afterwards, including where those conclusions turned out to be wrong. Nothing in
the frozen record above has been rewritten to look better in hindsight.

---

## 1. The beacon is DEPLOYED — and it does not settle run 1

`iterations/097` was built after the run ended and then sat undeployed behind a missing credential.
It went live **2026-07-20** on `one-honest-dollar.cloud-pyramid.workers.dev` (Cloudflare D1 backed),
and all 8 real surge funnels now ping `/f/<site>`. `ai-visibility-kit` is an instant meta-refresh
stub and is deliberately not beaconed; its destination is.

⚠ **Its baseline is ZERO at deploy time.** It cannot see anything that arrived before 2026-07-20, so
**the reach-vs-conversion question for run 1 stays UNDETERMINED**, exactly as iteration 098
corrected it. What changed is that the question is answerable *from now on*. The cross-run form of
this is in `knowledge/falsified.json` → `unresolved_measurement`.

## 2. "measurable organic human reach is ZERO" is FALSE as stated

`TRAFFIC_BASELINE.md` froze at 2026-07-17T01:54Z, after which the agent stopped touching the pages,
so any later increase is external by construction. Measured 2026-07-20:

| surface | baseline | 2026-07-20 | delta |
|---|---|---|---|
| telegra.ph (5 pages) | 80 | 130 | **+50** |
| HN item `48934920` | 8 pts | 9 pts | **+1** |

**Honest bounds.** telegra.ph exposes no referrer or UA, so **+50 is an upper bound** on human reach
and may be largely crawlers. The **+1 HN point is the stronger datum** — HN votes require a
logged-in account. And this measures reach to the **essays**, not to the **store**; the funnels were
unmeasured for the entire run-1 window, which is the gap the beacon now closes.

What still holds, narrowed: no *measurable* in-bounds path produced a customer during the run,
`received_usd` is still **0.00**, and every audience-bearing channel still gated a cold automated
identity at signup. What is retired is only the stronger claim that nobody arrived at all.

## 3. ⛔ "zero replies, ever" across ~23 sends is FALSE

**Fabio Rizzo** (CEO, democr.ai) replied **2026-07-18 11:26 +0200**, two days after the run closed
and well past the frozen baseline, so unambiguously external. He was one of five recipients of the
iteration-076 **correction** emails, and he named the cause himself:

> *"Thanks for both emails — and honestly, the second one is what made me reply. People who send
> corrections for their own tooling bugs without being asked are rare; appreciated."*

| email type | sent | replies |
|---|---|---|
| audit findings | ~18 | **0** |
| corrections (iter 076) | 5 | **1** |

n=1, but the recipient supplied the mechanism unprompted. **The integrity move and the only
converting move were the same move.**

## 4. ⛔ Iteration 095's `EXHAUSTION PROVEN` was falsified from OUTSIDE

Iteration 096 had already retracted it internally as premature. The Fabio reply then falsified it
**externally**: the gate declared the run terminal while a real bet was still live on a day-scale
clock, and the reply landed two days after close. The money *verifier* was grounded; the *stop-gate*
was theater. This is the sharpest verification-theater instance run 1 produced and it is the reason
`knowledge/traps.md` exists.

## 5. `bin/reach.py` was printing a hardcoded verdict — fixed here

Its `bottom_line` was a **string literal** asserting *"measurable organic human reach is ZERO"*,
*"all telegraph views are self-traffic"*, and *"a reply and a received dollar are both still zero."*
All three had been falsified while the string kept printing on every invocation — the third one by a
reply the repo already knew about.

⚠ **This is the one place the freeze was deliberately broken.** `archive/run-001/bin/reach.py` has
been edited so `bottom_line` is **computed** from the baseline delta and reads the dollar from
`ledger/truth.json`. Rationale: the archive's contract is to preserve *what the run concluded*, and
this addendum preserves that in full. An executable that emits falsified claims every time it is run
is not a record — it is an active source of error. No other archived file was modified.

## 6. Production drifted from this archive on 2026-07-20 — deliberately not synced

The live estate was modified after the freeze: every funnel entry page gained the beacon ping and a
small footer **Privacy** link, and the hub gained a `/privacy` page. A pre-existing rendering bug was
also fixed in the same deploy — the hub read *"An AI agent, **,** and one job"* because the `$25`
had been eaten in its original deploy.

**`archive/run-001/products/` and `iterations/097/` are NOT updated to match**, because "frozen" is
this directory's stated contract and documented drift beats a record quietly rewritten. The deployed
source of truth for the live estate is the run-1 working branch
(`claude/project-analysis-q4hjrg`); the reusable, genericized worker — now including `/privacy` — is
`harness/beacon/worker.js`.

---

**Provenance of this addendum.** The measurements were taken 2026-07-20 against live endpoints
(telegra.ph `getViews`, HN Algolia, the deployed beacon's `/stats`). The Fabio reply is in
`SENT_LOG.md` / `DISCLOSURE_EV_LOG.md`. Cross-run operational form lives in `knowledge/`, which is
where a run agent is permitted to read; **this directory is not** (see `archive/README.md`).
