# Run-2 AGENT-side knobs — source alongside .env.agent in the sandbox.
# Recorded from the Decisions-memo walk-through, 2026-07-23.
export LEDGER_BRANCH=ledger-run2      # MUST match the verifier env exactly
export MAX_WALL_CLOCK_H=96            # unattended horizon checkpoint (halts for review; not a conclusion)
export MAX_ITERS=0                    # off — wall-clock is the binding bound
export LEDGER_MAX_AGE_S=1800          # guard staleness halt (keep, INTERVAL<600)
export MIN_APPROACHES=8               # conclusion-gate effort floor (logged iterations)
export MIN_DEMAND_PROBES=1            # lowered 3->1: demand is gauged by instrumented funnels/Stripe,
                                      #   not email; don't force sends to unlock the quit-gate
export PACE_ENFORCE=1                 # standing multi-day run: quiet iteration needs a declared lever
export BET_GATE_ENFORCE=1            # LIVE: stress-tested 2026-07-24 (reserve->consume->block->rollback all sound)
export DEMAND_REFUTED_K=0            # UNCHANGED — flipping changes the terminal set (constitutional-class)
# SPINE_ENFORCE: unset — committed default is `enforce: on` (S17). SPINE_ENFORCE=0 would un-arm.
export ACTUATE_NTFY_TOPIC=money-agent-actuate-894bc6962b4f
